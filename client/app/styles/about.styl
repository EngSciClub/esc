/* About, the about page styles.
 * author: John Liu
 */

ul.team {
  list-style-type: none;
  padding: 0;
}

.people {
  @extends .row;

  margin-bottom: 1em;

  .clear {
    clear: both;
  }
}

.social-icons {
  font-size: 1.2em;
  i {
    margin: 0 0.25em;
  }
}
