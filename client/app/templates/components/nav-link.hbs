<a {{bind-attr href=fixedPath}}>
    {{#if route.icon}}
        <i {{bind-attr class=route.icon}}></i>
    {{/if}}
    {{route.name}}
</a>

{{! Recursively build routes if they're expandable. }}
{{#if route.expandable}}
    {{#if route.routes}}
        <div class="chevron" {{action "expand" bubbles=false}}></div>
        {{nav-bar routes=route.routes
                  currentPath=currentPath
                  expanded=expanded
                  class="secondary"
                  action="selectRoute"}}
    {{/if}}
{{/if}}
